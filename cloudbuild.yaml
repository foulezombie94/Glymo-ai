steps:
  # Step 1: Build the Docker image
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "europe-west1-docker.pkg.dev/$PROJECT_ID/glymo-repo/glymo-app", "." ]

  # Step 2: Push the image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "europe-west1-docker.pkg.dev/$PROJECT_ID/glymo-repo/glymo-app"]

  # Step 3: Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "glymo-app"
      - "--image"
      - "europe-west1-docker.pkg.dev/$PROJECT_ID/glymo-repo/glymo-app"
      - "--region"
      - "europe-west1"
      - "--allow-unauthenticated"

images:
  - "europe-west1-docker.pkg.dev/$PROJECT_ID/glymo-repo/glymo-app"

options:
  logging: CLOUD_LOGGING_ONLY
